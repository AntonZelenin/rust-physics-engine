use crate::fireworks_demo::firework::Firework;

fn create(rule: FireworkRule, parent: &Firework) -> Firework {
//    let rule = &self.rules[(firework_type - 1) as usize];
//    rule.create(&self.fireworks[self.next_firework as usize], Some(&parent));
//        self.next_firework = (self.next_firework + 1) % self.max_fireworks;
}